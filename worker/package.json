{
	"name": "@cube-codes/cube-codes-ide-worker",
	"version": "1.0.0",
	"type": "commonjs",
	"scripts": {
		"build": "tsc",
		"test": "jest",
		"build-browser": "rollup -c",
		"ci-build": "npm ci && npm run build && npm run test && npm run build-browser",
		"ci-release": "npm run ci-build"
	},
	"devDependencies": {
		"@rollup/plugin-commonjs": "^15.0.0",
		"@rollup/plugin-node-resolve": "^9.0.0",
		"@types/jest": "^26.0.13",
		"jest": "^26.4.2",
		"rollup": "^2.26.11",
		"ts-jest": "^26.3.0",
		"typescript": "^4.0.2"
	},
	"dependencies": {
		"@cube-codes/cube-codes-model": "0.0.26"
	}
}
